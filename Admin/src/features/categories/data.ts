import type { Category } from './types';

export const ITEMS_PER_PAGE = 20;

// Clean static array; all categories are fetched dynamically from live API endpoints
export const INITIAL_CATEGORIES: Category[] = [];
